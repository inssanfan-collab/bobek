/**
 * Имя cookie сессии вынесено отдельно от session.ts: тот помечен server-only,
 * и сквозные тесты его импортировать не могут.
 *
 * Раньше имя было вписано в тесты строкой. При переименовании бренда оно
 * разъехалось с кодом, тесты искали несуществующую cookie и падали уже в CI —
 * ровно тот случай, ради которого константа и нужна в одном месте.
 */
export const SESSION_COOKIE = 'edusad_session';
