-- Демонстрационный сад (demo.edusad.kz): сайт открыт, но в каталог портала,
-- счётчики садов, ленту новостей и карту сайта портала он не попадает —
-- родитель не должен принять выдуманный сад за настоящий.

ALTER TABLE "Tenant" ADD COLUMN "isDemo" BOOLEAN NOT NULL DEFAULT false;
