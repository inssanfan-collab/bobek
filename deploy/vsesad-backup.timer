[Unit]
Description=Копия базы EduSad каждую ночь в 03:30

[Timer]
OnCalendar=*-*-* 03:30:00
# Сервер был выключен в 03:30 — копия сделается сразу после включения.
Persistent=true

[Install]
WantedBy=timers.target
